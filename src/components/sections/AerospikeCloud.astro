---
import Container from "../shared/Container.astro";
import SectionHeading from "../shared/SectionHeading.astro";
import SectionName from "../shared/SectionName.astro";
---

<Container>
  <section class="aerospike-cloud">
    <div class="texts">
      <SectionName>Why AerospikeCloud</SectionName>
      <SectionHeading>Scalable, by every definition</SectionHeading>
    </div>

    <div class="wrapper">
      <div class="content">
        <h3>Avoid hyperscaling invoices</h3>
        <p class="description">
          Do more, without paying per transaction<br />
          <span
            >Unlike cloud-vendor databases, Aerospike Cloud doesn’t charge for
            transactions.</span
          >
        </p>

        <h4>Avoid cloud-vendor lock-in</h4>
        <p class="description">
          Run on AWS or GCP today—switch clouds or choose your own data center
          in the future.
        </p>

        <a href="#" class="report-button">
            <span>Read the Aerospike vs DynamoDB report</span>
           <span class="icon">→</span>
        </a>
      </div>

      <div class="image-block">
        <img src="/images/aerospike-cloud.png" alt="Aerospike Cloud" />
      </div>
    </div>
  </section>
</Container>

<style>
  section.aerospike-cloud {
    padding: 60px 0;
  }

  .texts {
    display: flex;
    flex-direction: column;
    gap: 20px;
    text-align: center;
    margin-top: 160px;
  }

  .wrapper {
    display: flex;
    gap: 40px;
    align-items: center !important;
    justify-content: center;
    margin-top: 100px;
  }

  .content {
    max-width: 600px;
    display: flex;
    flex-direction: column;
    gap: 40px;
  }

  .content h3{
    color; #1A1A1A;
    font-size: 32px;
    font-weight: 500;
  }

  .description {
    font-size: 1rem;
    color: #555;
  }

  .description span {
    color: #999;
  }

  .report-button {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 20px;
    padding: 22px 20px;
    background: #f1f1f1;
    color: #000;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
  }

  .report-button .icon {
    font-size: 1.5rem;
    margin-left: 10px;
    color: #fff;
    background: #000;
    padding: 10px;
    border-radius: 10px;
    transition: transform 0.3s ease;
  }
  .report-button:hover .icon {
    transform: translateX(5px);
  }

  .image-block img {
    max-width: 100%;
    border-radius: 12px;
  }

  @media (min-width: 1024px) {
    .wrapper {
      flex-direction: row;
      justify-content: space-between;
      align-items: flex-start;
      text-align: left;
    }

    .content {
      flex: 1;
    }

    .image-block {
      flex: 1;
      padding-left: 40px;
    }
  }
</style>
